<div class="box">
    <article class="media">
        <div class="media-content">
            <div class="content">
                <p>
                    <strong><%= comment_sections.name %> :</strong>
                    <%= comment_sections.comment %>
                </p>
            </div>
        </div>

        <%= link_to 'Delete', [comment_sections.post, comment], method: :delete class: "button is-danger", data: { confirm: "Are you sure?" } %>
    </article>
</div>